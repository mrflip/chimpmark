
Proposed layout:

<pre><code>

/data
  /source                                                -- source holds data from elsewhere, not yet useful to us
    /wikidata/*
  /rawd/wp                                               -- rawd holds the raw data from which everything else comes
     /wikistats/pagecounts                               -- one page-day-hour count per line, tsv
     /wikistats/projectcounts                            -- one project-day-hour count per line, tsv
     /wikidump/articles/enwiki-latest-pages-articles-xml -- flattened xml fragments, one per articl
     /linkgraph                                          -- adjacency pairs graph, tsv
     /pageinfo                                           -- maps ids to page info, tsv
 
  /basic
  /graph
  /stats
  /text
     ... [each organized like the challenges dir]


</code></pre>     
